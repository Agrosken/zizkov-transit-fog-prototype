{
  "name": "zizkov-transit-fog-prototype",
  "private": true,
  "version": "0.0.1",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "fetch-gtfs": "node scripts/download-gtfs.js",
    "prepare-data": "node scripts/parse-gtfs.js && node scripts/build-transit-lines.js",
    "deploy": "vite build && gh-pages -d dist"
  },
  "dependencies": {
    "@turf/turf": "^7.1.0",
    "maplibre-gl": "^4.7.1"
  },
  "devDependencies": {
    "adm-zip": "^0.6.0",
    "csv-parse": "^5.5.6",
    "gh-pages": "^6.3.0",
    "vite": "^5.4.10"
  }
}
